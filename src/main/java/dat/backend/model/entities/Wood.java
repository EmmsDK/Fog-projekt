package dat.backend.model.entities;

public class Wood extends BuildingMaterial{
    public Wood(int price, int width, int length, String name, String unit) {
        super(price, width, length, name, unit);
    }
}
